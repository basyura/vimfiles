bufio.NewWriter(
cmd.StderrPipe()
cmd.StdinPipe()
cmd.StdoutPipe()
const
exec.Command(
filepath.Abs(
filepath.Dir(
filepath.Glob(
filepath.Join(
fmt.Fprintln(
fmt.Printf(
fmt.Println(
import
io.Reader
io.Writer
isatty.IsTerminal(
net.Dial
nil
os.Open(
os.Stderr
os.Stdin
os.Stdout.Fd()
package
strings.HasPrefix(
strings.HasSuffix(
strings.Repeat(
strings.Split(
time.Now()
regexp.MustCompile(
filepath.Glob(
flag.Bool(
flag.BoolVar(
flag.Parse()
flag.Args
strings.TrimSpace(
