
snippet     main
prev_word   '^'
    static void Main(string[] args) {
        ${1}
    }

snippet     class
abbr        class
prev_word   '^'
    class ${1:Class1} {
        ${2}
    }

snippet     tryInvokeMember
prev_word   '^'
    public override bool TryInvokeMember(InvokeMemberBinder binder, object[] args, out object result) {
        ${1}
    }

snippet     foreach
prev_word   '^'
    foreach (${1:Type} ${2:val} in ${3:Collection}) {
        ${4}
    }
